# Freesound API credentials.
#
#     cp .env.template .env
#
# Then fill in the two values from https://freesound.org/apiv2/apply/, which shows both
# once you have a Freesound account and have applied for a key. `.env` is gitignored.
# Never commit it.

FREESOUND_CLIENT_ID=
FREESOUND_API_KEY=
